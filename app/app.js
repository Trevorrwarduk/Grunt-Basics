/**
 * The main controller of the application.
 *
 *
 */

/**
 * @class
 * @ingnore
 */
var BaseApp = 'A Main Variable';

/**
 * @function
 *
 * This is the main function to initialise all parameters at load time..
 */
var init = function(inParams) {
  // A function to initialise the application

  var a = 2 + 2;
}
